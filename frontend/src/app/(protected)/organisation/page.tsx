import React from "react";
import Organisation from "@/components/organisation";

export const metadata = {
  title: "Organisation",
  description: "Manage your organisation's settings and configurations.",
};

function Page() {
  return <Organisation />;
}

export default Page;
