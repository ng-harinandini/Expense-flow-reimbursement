# LOG

> Append-only, chronological. Newest entries at the bottom. One line or short block per event.
> Record: baselines, verification runs, branch creation, task status flips, bugs, handoffs.

Format: `YYYY-MM-DD HH:MM | <hook/skill> | <event> | <result>`

---

2026-07-24 | bootstrap | Created workflow framework (CLAUDE.md, HANDOFF.md, LOG.md, TASKS/task.md, PLANS/, DECISIONS/, 6 hooks, 11 skills) | done
2026-07-24 | task-breakdown | Created Primary Task T001 (Receipt Upload & Amazon Textract Extraction API) + Macro/Atomic breakdown; registered in task.md | done
2026-07-24 | hook-1-baseline | Baseline for T001: `python -c "from app.main import app"` exits 0, app imports/boots with routers registered, no errors. boto3 NOT installed in venv (expected new dep); python-multipart already present. No test suite exists → Done Checks are boot + endpoint observation. No pre-existing failures to carry forward. | pass
2026-07-24 | hook-2-planning | Wrote PLANS/plan-T001.md (status awaiting-approval). Paused at human approval gate before Branch/Build. | awaiting-approval
2026-07-24 | hook-2-planning | REVISED plan T001 per owner: scope expanded from in-memory to PostgreSQL persistence (SQLAlchemy 2.x, Alembic sole schema owner, pydantic-settings), S3 for original documents, Textract AnalyzeExpense. Added DB-safety constraints (no create_all/drop/truncate; STOP on schema conflict). Re-requesting approval. | awaiting-approval
2026-07-24 | hook-2-planning | Reconciled PLANS/plan-T001.md: rewrote stale in-memory draft to match revised PostgreSQL/S3/Textract scope in task-T001 detail file. Plan + task detail now consistent. Still paused at human approval gate; no Build started. | awaiting-approval
2026-07-24 | approval | OWNER APPROVED plan T001. Directive: build first (fallback mode, no AWS/DB creds needed to boot); owner will supply AWS credentials + DATABASE_URL later via env/credential chain. Proceeding to Hook 3 Branch → Hook 4 Build. | approved
2026-07-24 | hook-3-branch | git init at expense root; created branch feature/T001-receipt-textract-postgres; added root .gitignore (venv/node_modules/.next/.env). No commits yet (commit only on owner request). | done
2026-07-24 | hook-4-build | Started Build for T001. | in-progress
2026-07-24 | hook-4-build | Built all atomic tasks M1-A1..M7-A3: requirements (sqlalchemy/alembic/psycopg/pydantic-settings/boto3), pydantic-settings config w/ DATABASE_URL||DB_* resolver, .env.example (placeholders), database.py (lazy engine, pool_pre_ping, no create_all), models (receipts/receipt_fields/receipt_line_items + extraction_status enum), alembic (ini/env/mako + initial migration 0001), s3_service + textract_service (graceful fallback), receipt schemas, receipts API (upload/list/get), health /ready + router registration. | done
2026-07-24 | hook-5-verify | Installed deps into venv. Boot: `from app.main import app` exits 0 (no schema touch). OpenAPI lists POST /api/receipts/upload, GET /api/receipts, GET /api/receipts/{id}, GET /api/health, GET /api/ready. Models map; Base.metadata has 3 tables; no create_all in app code. S3+Textract fallback return correct shape. TestClient: /health 200 ok; /ready 503 not_ready (DB unconfigured — correct). alembic history shows 0001_initial_receipts (head). Offline `alembic upgrade head --sql` emits CREATE TYPE enum + 3 tables + indexes + FK ON DELETE CASCADE. | pass (non-DB checks)
2026-07-24 | hook-5-verify | DEFERRED (need live DATABASE_URL + optional AWS creds from owner): `alembic upgrade head`/`current`/`downgrade -1` against real DB; POST /upload → 201 persisted row; GET list+by-id from DB; /ready healthy. Owner will supply creds later per approval directive. | deferred
2026-07-24 | decision-logging | Wrote DECISIONS/ADR-001-receipts-persistence-architecture.md. | done
2026-07-27 | analysis | Audited all API routes vs. roles. Finding: RBAC documented in README/frontend matrix but NOT enforced server-side — 15 endpoints all unauthenticated; actorRole read from body, never validated. | noted
2026-07-27 | task-breakdown | Created Primary Task T002 (JWT Authentication & RBAC Enforcement) + M1..M5 breakdown; registered in task.md. Owner decisions: real JWT/session auth; enforce employee ownership of claims & receipts. | done
2026-07-27 | hook-1-baseline | Baseline for T002: `from app.main import app` boots OK; migration head 0001_initial_receipts. Confirmed zero auth/authorization on all routers (code read). No test suite for auth yet. No pre-existing auth failures to carry. | pass
2026-07-27 | hook-2-planning | Wrote PLANS/plan-T002.md (status awaiting-approval). One open decision at approval gate: user/credential store location (recommended in-memory seeded vs. Postgres users table). Paused at human approval gate before Branch/Build. | awaiting-approval
2026-07-27 | hook-2-planning | REVISED T002 per owner: auth now via AWS Cognito (User Pool owns identity/tokens; backend verifies Cognito JWT). Roles = Cognito Groups (cognito:groups); employee link = custom:employeeId; login proxy via boto3 initiate_auth. Dropped local user table/passlib/Alembic users migration (no DB coupling). Reconciled task-T002 detail file to match. New open questions: provisioning (boto3 script vs IaC), token acquisition (login proxy vs Hosted UI), live-vs-deferred verification. Still paused at approval gate. | awaiting-approval
2026-07-27 | git-commit | Discovered TWO repos: outer `expense/` (workflow docs, 0 commits) and nested `Expense-flow-reimbursement/` (the app, own repo, branch feature/backend-api). T001 CODE was also uncommitted. Prevented a broken submodule gitlink by adding `Expense-flow-reimbursement/` to outer .gitignore; fixed recursive __pycache__ ignore in app repo. Repos remain independent (flatten deferred — needs explicit owner go-ahead). | noted
2026-07-27 | git-commit | App repo (Expense-flow-reimbursement, branch feature/backend-api): committed T001 code as e45b404 "Add receipt upload, Textract extraction and PostgreSQL persistence" (on top of prior Initial commit / Updated README.md). | done
2026-07-27 | git-commit | Outer repo (expense, branch feature/T001-receipt-textract-postgres): first commits ever — ce1341f "Add file-backed workflow framework and T001 receipts task records"; b218bb6 "Add T002 auth/RBAC task and plan (AWS Cognito approach)". Summary-only messages per CLAUDE.md. Nothing pushed. | done
2026-07-27 | hook-3-branch | App repo (Expense-flow-reimbursement): created branch feature/T002-auth-rbac off e45b404. | done
2026-07-27 | hook-4-build | T002: integrated custom:role_id into login. auth.py now decodes the Cognito ID token (unverified — token just received over TLS) and returns a `user` {sub,email,role=custom:role_id,employeeId=custom:employeeId,groups}. Added AuthenticatedUserSchema. NOTE design divergence: role now from custom:role_id attribute, not Cognito Groups (ADR-002/plan still say groups — to reconcile). | done
2026-07-27 | hook-5-verify | Added tests/test_auth_login.py (mocked boto3): 11 unit tests PASS (success+role extraction, 401 bad creds, NEW_PASSWORD_REQUIRED challenge, 400 invalid-param, 502 no-creds, SECRET_HASH, 503 unconfigured, pool-status). Live test opt-in via COGNITO_TEST_EMAIL/PASSWORD env (creds never committed). Added pytest to requirements. | pass (unit)
2026-07-27 | design-change | Owner: custom:role_id is the SINGLE SOURCE OF TRUTH for RBAC; Cognito Groups no longer used. Reconciled ALL docs (task-T002 constraints/done-checks/M1-M5/matrix, plan-T002 approach/token-model/build-order/done-checks, ADR-002 decision+alternatives) from groups → custom:role_id. custom:employeeId stays ownership link; sub stays canonical id + audit targetId; app client must have read perms for both custom attrs. | done
2026-07-27 | hook-4-build | T002 M2-verify + M3: added app/core/security.py (JWKS fetch/cache + RS256 verify: iss/aud/token_use=id/exp) and app/core/deps.py (get_current_user → 401 bad/missing token, 403 missing/invalid custom:role_id; require_roles(*roles) → 403; authorizes exclusively from custom:role_id, groups ignored). Dropped cognito:groups from login user object (auth.py + AuthenticatedUserSchema). Added python-jose[cryptography] to requirements. | done
2026-07-27 | hook-5-verify | Added tests/test_auth_rbac.py (offline, local RSA keypair stands in for pool JWKS): 6 verify tests (valid/expired/wrong-aud/wrong-iss/access-token/tampered) + 7 authz tests (success, no-token 401, bad-token 401, missing-role 403, invalid-role 403, require_roles allow, require_roles forbid 403). Full suite: 24 passed, 1 skipped (opt-in live login). | pass
2026-07-28 | hook-4-build | T002 M4 (enforcement + ownership): wired require_roles onto every router per the matrix — claims (GET employee→own; POST employee-only + employeeId bound to token; action manager/finance/admin, DISBURSE finance/admin only, actor from token not body), receipts (upload employee + employeeId bound; GET list employee→own; GET by id 404 on cross-employee), ai (ocr any-auth, policy-reasoning manager/finance/admin, refine-iam admin), policy-rules (GET any-auth, PUT finance/admin), audit-logs (finance/admin/auditor), aws export-code (admin). Added GET /api/auth/me (any-auth); gated /api/auth/pool-status to admin. Health/ready/root stay public. | done
2026-07-28 | hook-4-build | T002 M5 (admin user-management): app/api/admin_users.py (prefix /api/admin/users, all require_roles("admin")) — POST create (admin_create_user, sets custom:role_id + custom:employeeId, FORCE_CHANGE_PASSWORD invite), GET list (paginated), GET/{email}, PATCH/{email}, POST/{email}/role (validates value ∈ 5 roles then admin_update_user_attributes custom:role_id — no groups), enable, disable. Keyed by normalized email; audits via add_audit_log with sub as targetId. Shared app/services/cognito.py client factory. Registered in main.py (9th router). | done
2026-07-28 | hook-5-verify | Added tests/test_admin_users.py (12) + tests/test_enforcement.py (7); fixed 2 pool-status tests for the new admin gate. Full suite: 41 passed, 1 skipped (opt-in live login). App boots, 22 OpenAPI paths. | pass
2026-07-28 | hook-4-build | T002 M1: wrote idempotent scripts/provision_cognito.py — reuses existing pool/client (no recreation), ensures custom:role_id + custom:employeeId attrs, merges ALLOW_USER_PASSWORD_AUTH into ExplicitAuthFlows (read-modify-write, preserves other client config), leaves ReadAttributes untouched when unset (=read-all), and verifies/sets BOOTSTRAP_ADMIN_EMAIL custom:role_id=admin without recreating or touching its password. No Cognito Groups. | done
2026-07-28 | hook-5-verify | Ran provision_cognito.py against live pool ap-south-1_uZwsnjASV: added custom:employeeId, enabled USER_PASSWORD_AUTH, set custom:role_id=admin on harinandini@ngenux.com. Second run = all no-ops (idempotent confirmed). Discovered app client HAS a secret → backend needs COGNITO_APP_CLIENT_SECRET (not in .env). LIVE login (secret supplied via env): POST /api/auth/login → 200 NEW_PASSWORD_REQUIRED challenge (auth flow + SECRET_HASH + creds all verified; tokens gated on first-login password change). | pass
2026-07-28 | hook-4-build | T002: added POST /api/auth/respond-challenge (completes NEW_PASSWORD_REQUIRED etc. via respond_to_auth_challenge → tokens; SECRET_HASH-aware; maps InvalidPassword→400, expired/invalid session→401, follow-on challenge surfaced). Added RespondChallengeRequestSchema. Persisted COGNITO_APP_CLIENT_SECRET to gitignored backend/.env (value not shown) so the secret-bearing client works. | done
2026-07-28 | hook-5-verify | Added 4 respond-challenge tests (success+role, invalid-password 400, expired-session 401, follow-on challenge). Full suite: 45 passed, 1 skipped (opt-in live login). 4 auth paths in OpenAPI. | pass
2026-07-28 | hook-6-docs | T002 M6: corrected frontend API matrix (ArchitectureOverview.tsx) to real backend paths/roles incl. auth + admin/users; expanded README with Auth & RBAC section (custom:role_id single source of truth, Cognito Groups unused, ownership via custom:employeeId, admin-driven onboarding, provisioning, .env config). | done
2026-07-28 | bug-fix | Live respond-challenge failed InvalidParameterException — root cause: pool requires 'name' but bootstrap admin lacked it (NEW_PASSWORD_REQUIRED demands missing required attrs). Immediate fix: set name=Harinandini on admin; full login→challenge→tokens then verified (user.role=admin). | fixed
2026-07-28 | hardening | 3 fixes so it can't recur: (1) /auth/respond-challenge accepts optional name + userAttributes and passes them as userAttributes.* in ChallengeResponses (schema RespondChallengeRequestSchema extended); (2) provision_cognito.py sets 'name' on bootstrap-admin create and repairs missing name on existing (BOOTSTRAP_ADMIN_NAME env, defaults to email local-part; .env.example updated); (3) respond-challenge surfaces Cognito's real Error.Message instead of a generic string. Added 2 tests (name passthrough, real-message surfaced). Suite: 47 passed, 1 skipped. | done
2026-07-27 | hook-5-verify | LIVE login for harinandini@ngenux.com FAILED 401 — diagnosed against pool ap-south-1_uZwsnjASV: (a) app client ExplicitAuthFlows lacks ALLOW_USER_PASSWORD_AUTH (only REFRESH/USER_AUTH/USER_SRP) → proxy flow rejected; (b) user status FORCE_CHANGE_PASSWORD (temp password); (c) custom:role_id unset on user; (d) app client ReadAttributes excludes custom:role_id. CORRECTION: my prior claim that USER_PASSWORD_AUTH was enabled was wrong (the earlier 401 was 'auth flow not enabled'). Cognito-side config fixes required before live login succeeds. | blocked (owner Cognito config) |
2026-07-27 | hook-4-build | T002 M2 (partial — login proxy): added Cognito settings to config.py (COGNITO_USER_POOL_ID/APP_CLIENT_ID/APP_CLIENT_SECRET; region auto-derived from pool-id prefix), Login schemas, app/api/auth.py (POST /auth/login via boto3 initiate_auth USER_PASSWORD_AUTH with SECRET_HASH + challenge handling; GET /auth/pool-status via describe_user_pool), registered router in main.py, created backend/.env.example. Graceful 503 when unconfigured. Boot OK; OpenAPI shows /api/auth/login + /api/auth/pool-status. LIVE-VERIFIED against owner pool ap-south-1_uZwsnjASV: pool-status 200 reachable (0 users); login bad-creds -> 401 (boto3 reached Cognito, USER_PASSWORD_AUTH enabled). NOT yet built: token verification/JWKS (M3), RBAC enforcement (M4), admin user-mgmt (M5), tests+docs (M6). | pass
2026-07-27 | hook-2-planning | REVISED T002 per owner (plan approved): admin-driven Cognito user provisioning. Provisioning seeds only pool/client/groups + one bootstrap admin (BOOTSTRAP_ADMIN_EMAIL, env; no password in code/logs/CLI); onboarding = Admin → FastAPI → boto3 → Cognito via new admin-only router app/api/admin_users.py (create/list/get/update/role/enable/disable, keyed by normalized email). Email = sign-in alias + admin-facing id; Cognito sub = canonical internal identity (audit targetId). Role change touches only the 5 app-role groups, verifies exactly one remains. Added macro M5 (admin router), renumbered verify+docs to M6, added Done Checks 9-12 + 7 matrix rows. Updated plan-T002.md + task-T002; wrote DECISIONS/ADR-002-auth-rbac.md. Frontend/README doc edits deferred to T002 build (M6). Still 2 open questions at approval gate (provisioning mechanism, token acquisition). | done
